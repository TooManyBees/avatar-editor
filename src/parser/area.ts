import { AreaSection } from "../app/models";

export default function parseArea(section: string): AreaSection {
	let area = {
		levelRange: "",
		author: "",
		name: "",
		_error: {},
	};
	
	return area;
};
